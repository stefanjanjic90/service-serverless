import { ConnectorApi } from './connector';

export const JiraConnectorApi = "JiraConnector";

/**
 * Defines authentication and authorization connector for JIRA API.
 */
export interface JiraConnectorApi extends ConnectorApi {
}